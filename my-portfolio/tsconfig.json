{
  "files": [],
  "references": [
    {
      "path": "./tsconfig.app.json"
    },
    {
      "path": "./tsconfig.node.json"
    }
  ],
  "compilerOptions": {
    "jsx": "react-jsx", // Use this for React 17 or newer
    "baseUrl": "src", // This should point to the 'src' directory
    "paths": {
      "@/*": [
        "*"
      ] // This allows the use of '@/...' for imports
    }
  }
}